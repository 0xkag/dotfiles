install emacs
clone dotfiles repo
cd .dotfiles
git submodule update --init --remote --rebase
git submodule foreach git checkout master # TODO: fix this
TODO add dotfiles bin to path
bin/cask upgrade-cask
cd ~/.emacs.d
ln -sf ../.dotfiles/cask/Cask Cask
cask install
 
