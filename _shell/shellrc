# vim: et sw=4 ts=4 ft=sh:

[ -n ${DOTFILES_DEBUG:-""} ] && echo "dotfiles: shellrc"

case $- in
    *i*)
        source ~/.dotfiles/_shell/shellinteractive
        ;;
    *)
        source ~/.dotfiles/_shell/shellenv
        ;;
esac

