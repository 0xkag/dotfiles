#!/usr/bin/env bash

set -e
set -u
set -x
set -o pipefail

pushd .
cd $(dirname $0)
cd ../_lib/bup
git reset --hard
git clean -fdx
./configure
make PREFIX=~/.root && make PREFIX=~/.root install
popd

