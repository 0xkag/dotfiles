* _opt: Things that are optional (or only used when modern packages aren't
  available on $machine).

* _todo: Things I've flagged to use but haven't integrated yet.

<everything else>: Things used everywhere.

