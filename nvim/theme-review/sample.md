# Theme Review

This file is here to exercise **Markdown**, *italic text*, `inline code`, and
[a link](https://example.com/).

- [ ] Check list rendering
- [x] Check checkbox contrast
- Item with `SearchTarget`

## Wrap And Spell

This intentionally long paragraph should make it easy to inspect `colorcolumn`,
line wrapping, `showbreak`, and spell underlines for a misspeled wurd in the
middle of prose.

### Code Fence

Inline code stays here as `def hello(name): return name`.
