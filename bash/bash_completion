[ -f /usr/local/share/bash-completion/bash_completion.sh ] && \
    source /usr/local/share/bash-completion/bash_completion.sh

for i in $(find ~/.dotfiles/bash/bash_completion.d/ -type f); do
    source "$i"
done

